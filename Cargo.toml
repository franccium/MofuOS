[workspace]
members = ["kernel"]
exclude = ["xtask"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
authors = ["franccium"]
license = "MIT"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 3
lto = true
codegen-units = 1
